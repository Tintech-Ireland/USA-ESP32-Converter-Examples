# Console on the native USB-Serial-JTAG port (flash + monitor over one cable).
# The bridge uses the TWAI (CAN) peripheral, so the console choice does not conflict.
CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y
